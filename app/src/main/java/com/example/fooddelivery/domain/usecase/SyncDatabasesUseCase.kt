import com.example.fooddelivery.data.model.Category
import com.example.fooddelivery.data.model.Restaurant
//import com.example.fooddelivery.domain.respository.restoRepository
import com.example.fooddelivery.domain.respository.CategoryRepository


/*class SyncDatabasesUseCaseR(private val repository: restoRepository) {
    suspend operator fun invoke(remoteR: List<Restaurant>) {
        repository.syncResto(remoteR)
    }
}
/*class SyncDatabasesUseCaseC(private val repository: CategoryRepository ) {
    suspend operator fun invoke(remoteR: List<Category>) {
        repository.syncCategory(remoteR)
    }
}*/